spring:
  application:
    name: bank

  datasource:
    url: jdbc:mysql://localhost:3306/bank
    driver: com.mysql.cj.jdbc.Driver
    username: "root"
    password: "root123"

  jpa:
    hibernate:
      ddl-auto: create-drop